# from django.urls import path
# from .views import SignatureUploadView, SignatureVerifyView, SignatureListView

# urlpatterns = [
#     path('upload/', SignatureUploadView.as_view(), name='signature-upload'),
#     path('verify/', SignatureVerifyView.as_view(), name='signature-verify'),
#     path('all/', SignatureListView.as_view(), name='signature-list'),
# ]
